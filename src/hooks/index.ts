// src/hooks/index.ts - Export Barrel para Hooks

export { usePosts } from './usePosts';
export { useToast } from './useToast';

// Facilita imports limpos:
// import { usePosts, useToast } from '@/hooks';